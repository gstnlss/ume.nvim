-- Local configuration overrides for machine-specific settings
-- This file serves as a template for local-overrides.lua
--
-- Usage:
--   1. Copy this file to local-overrides.lua:
--      cp local-overrides.lua.example local-overrides.lua
--   2. Uncomment and modify the settings you want to override
--   3. The local-overrides.lua file is git-ignored and won't be committed
--
-- Note: This file is loaded by plugins that support local overrides.
--       If the file doesn't exist, default values will be used.

return {
  -- OpenCode configuration overrides
  opencode = {
    -- Custom command to launch opencode (e.g., for sandboxed/enterprise versions)
    -- Uncomment and set to your custom binary:
    -- command = "genai-opencode",

    -- You can also override other opencode settings here:
    -- auto_reload = false,
    -- auto_focus = true,
    -- win = {
    --   position = "bottom",
    -- },
  },

  -- Add other plugin overrides here as needed
  -- Example:
  -- lsp = {
  --   custom_server_path = "/path/to/custom/lsp",
  -- },
}
