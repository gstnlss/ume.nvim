-- nvim-lspconfig provides default configurations for LSP servers
-- This is loaded by lazy.nvim but does not execute any LSP setup
-- Actual LSP configuration happens in lua/ume/config/lsp.lua
return {
  "neovim/nvim-lspconfig",
}
